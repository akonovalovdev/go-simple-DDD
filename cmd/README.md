### This directory contains the entrypoints of the application.

Each subdirectory represents a separate binary — a runnable command that can be built and deployed independently.
Place your `main` packages here, one per executable (e.g., the main service, seed tools, migration runners, etc.).
